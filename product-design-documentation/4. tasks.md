### **Epic 1: Backend Infrastructure & Security**
*Focus: Setting up the Single Source of Truth (SSOT) and securing the connection.*

| Task ID | Component | Task Description |
| :--- | :--- | :--- |
| **BE-1.1** | Database | **Initialize MySQL Schema:** Create `families`, `users`, and `chores` tables with defined FK constraints and UUID primary keys. |
| **BE-1.2** | API | **Auth Middleware:** Implement a PHP script to validate the `X-CHORES-TOKEN` header on every incoming request. |
| **BE-1.3** | API | **Family/User Creation:** Develop `POST /api/family/create` and `POST /api/users/add` endpoints to register the initial household data. |
| **BE-1.4** | API | **Chore Fetching:** Implement `GET /chores.php` with a filter to exclude records where `deleted_at` is not null. |
| **BE-1.5** | API | **Claim Validation:** Add server-side logic to reject claim actions with a **409 Conflict** if the chore is already assigned to another user. |

---

### **Epic 2: Android Identity & Onboarding**
*Focus: "Who are you?" logic and persistent identity.*

| Task ID | Component | Task Description |
| :--- | :--- | :--- |
| **AND-2.1** | Data Layer | **Identity DataStore:** Implement `UserSessionManager` using Jetpack DataStore to save and retrieve the `activeUserId`. |
| **AND-2.2** | UI Layer | **Onboarding Screen:** Create a Compose-based user selection screen that triggers on the first launch if no `activeUserId` is found. |
| **AND-2.3** | UI Layer | **Global Navigation Bar:** Implement the `TopAppBar` that dynamically displays "Acting as: [Name]" fetched from the DataStore. |
| **AND-2.4** | UI Layer | **Switch User Action:** Add a "Switch User" button in the TopBar that clears the DataStore and returns the user to the selection screen. |

---

### **Epic 3: The Core Chore Loop**
*Focus: Universal management and the "Locked Ownership" claim system.*

| Task ID | Component | Task Description |
| :--- | :--- | :--- |
| **AND-3.1** | Domain | **Claim UseCase:** Create `ClaimChoreUseCase` to handle the logic of sending a claim request to the server. |
| **AND-3.2** | UI Layer | **Chore Card Component:** Build a reusable Compose card displaying title, due date, status, and the "I'll do it!" or "Done!" buttons. |
| **AND-3.3** | UI Layer | **Chore Creation Form:** Develop a dialog/form to input a Title, optional Description, and Due Date. |

---

### **Epic 4: Data Safety & Conflict Handling**
*Focus: Preventing data loss and resolving race conditions.*

| Task ID | Component | Task Description |
| :--- | :--- | :--- |
| **AND-4.1** | UI Layer | **Deletion Confirmation:** Implement a Material Design alert dialog that triggers when any user attempts to delete a chore. |
| **BE-4.2** | API | **Soft-Delete Logic:** Update the delete endpoint to set the `deleted_at` timestamp rather than purging the record. |
| **AND-4.3** | UI Layer | **Conflict Snackbar:** Implement an error handler in the ViewModel to display a Snackbar when a 409 Conflict is received from the API. |

---

### **Epic 5: Connectivity & Sync**
*Focus: Ensuring the app remains strictly online as the SSOT.*

| Task ID | Component | Task Description |
| :--- | :--- | :--- |
| **AND-5.1** | Remote | **Retrofit Integration:** Set up the network layer to include the hardcoded API token in all headers automatically. |
| **AND-5.2** | UI Layer | **Offline Blockade:** Implement a visual overlay or "No Connection" state that prevents users from interacting with chore cards when the device is offline. |